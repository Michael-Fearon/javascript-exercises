const repeatString = function(string, repeatN) {
    let i = 0;
    let outputString = "";

    while (i < repeatN){
        outputString += (string);
        i ++;
    }
    return outputString;
};

// Do not edit below this line
module.exports = repeatString;
